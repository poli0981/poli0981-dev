# Supply-chain hardening (ignore-scripts) is applied in CI only — see docs 06 §1.6.
# Locally we leave install scripts enabled so `sharp` can set up its binaries
# for build-time image processing (astro imageService: 'compile').
fund=false
